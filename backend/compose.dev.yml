# Development environment Docker Compose override
# Usage: docker compose --env-file .env.dev -f compose.yml -f compose.dev.yml up

services:
  web:
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - DJANGO_ENV=dev
      - PYTHONPATH=/app/src
    command: python manage.py runserver 0.0.0.0:8000
    stdin_open: true
    tty: true

  celery:
    build: .
    command: celery -A core worker -l info
    volumes:
      - .:/app
    environment:
      - DJANGO_ENV=dev
      - DJANGO_SETTINGS_MODULE=core.settings
      - PYTHONPATH=/app/src
    depends_on:
      - db
      - redis
    restart: unless-stopped

  celery-beat:
    build: .
    command: celery -A core beat -l info
    volumes:
      - .:/app
    environment:
      - DJANGO_ENV=dev
      - DJANGO_SETTINGS_MODULE=core.settings
      - PYTHONPATH=/app/src
    depends_on:
      - db
      - redis
    restart: unless-stopped

  db:
    ports:
      - "5455:5432"

  redis:
    ports:
      # Host port 6380 to avoid clashing with a system Redis already on 6379.
      # Containers still talk to each other internally on redis:6379.
      - "6380:6379"
